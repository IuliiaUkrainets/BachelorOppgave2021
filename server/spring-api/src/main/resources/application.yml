server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/uitt
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialog: org.hibernate.dialect.MySQL8Dialect

